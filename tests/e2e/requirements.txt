# E2E test dependencies for hermes-notification-bridge
# These are required for running the bidirectional messaging E2E tests

# Core testing framework
pytest>=7.0.0
pytest-asyncio>=0.21.0

# HTTP client
requests>=2.28.0

# Android Debug Bridge (ADB)
# Note: ADB is typically installed via Android SDK Platform Tools
# The tests assume `adb` is available in PATH

# Optional: for enhanced testing capabilities
# selenium  # For browser testing (if needed for OWUI)
# docker  # For managing Docker containers if not done externally

# Development/testing tools
black>=22.0.0  # Code formatter
flake8>=6.0.0  # Linter
mypy>=1.0.0  # Type checker
